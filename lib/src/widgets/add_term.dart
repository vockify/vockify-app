import 'package:flutter/material.dart';

class AddTermWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Term'),
      ),
    );
  }
}
