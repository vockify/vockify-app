import 'package:flutter/material.dart';

class EditTermWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edit Term'),
      ),
    );
  }
}
